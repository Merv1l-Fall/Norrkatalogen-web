export type ContactFormData = {
	name: string;
	email: string;
	message: string;
	consent: boolean;
};

export type MagazineInterestFormData = {
	companyName: string;
	name: string;
	address: string;
	postalCode: string;
	city: string;
	email: string;
	consent: boolean;
};
